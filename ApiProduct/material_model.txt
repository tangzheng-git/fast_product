class ApiUrl(BaseModel):
    name = models.CharField(max_length=100, null=True, verbose_name=u'名称')
    url = models.CharField(max_length=100, unique=True, verbose_name=u'绝对路径')
    is_auth = models.BooleanField(default=False, verbose_name=u'是否需要授权')